import React from 'react';
import tw, { styled } from 'twin.macro';

import DocsLayout from '@/components/layouts/DocsLayout';

const AlertPage = () => {
  return <DocsLayout>AlertPage</DocsLayout>;
};

export default AlertPage;

const Wrapper = styled.div`
  ${tw``};
`;
